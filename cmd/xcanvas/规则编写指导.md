# CodeCanvas 规则编写指导

## 规则配置示例（YAML）

```yaml
# rules/java.yml
name: Spring Boot
type: framework
language: Java
levels:
  L1:
    paths: ["pom.xml", "build.gradle"]
    contains: ["spring-boot-starter"]
    extract_version_from_text:
      pattern: 'spring-boot-starter[^\\d](\\d+\\.\\d+\\.\\d+[a-zA-Z0-9\\.-])'
  L2:
    paths: ["/Application.java"]
    contains: ["@SpringBootApplication"]

name: log4j-checker
type: component
language: Java
levels:
  L1:
    paths: ["pom.xml"]
    contains: ["log4j-checker"]
    extract_version_from_text:
      pattern: '<artifactId>log4j-checker</artifactId>\\s<version>([^<]+)</version>'
  L3:
    paths: ["/log4j-checker-.jar"]
    extract_version_from_path:
      pattern: "log4j-checker-([\\d.]+[a-zA-Z0-9\\.-]*)\\.jar"
```

💡 引擎按 L1 → L2 → L3 顺序尝试，取第一个命中的结果。
